import { HeroSection } from "@/components/home/hero-section";
import { StatsBar } from "@/components/home/stats-bar";

export default function HomePage() {
  return (
    <main>
      <HeroSection />
      <StatsBar />
      {/* Services bento grid — Phase 2, Step 3 */}
      {/* Featured classes — Phase 2, Step 4 */}
      {/* Trainer spotlight — Phase 2, Step 5 */}
      {/* Testimonials — Phase 2, Step 6 */}
      {/* Final CTA banner — Phase 2, Step 7 */}
    </main>
  );
}